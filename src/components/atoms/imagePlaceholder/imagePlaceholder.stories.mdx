import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, text, select } from '@storybook/addon-knobs';
import styled from '@emotion/styled';

import ImagePlaceholder from './ImagePlaceholder.js';

<Meta
  title="Atoms/ImagePlaceholder"
  component={ImagePlaceholder}
  decorators={[withKnobs]}
/>

# ImagePlaceholder

Das Projekt beinhaltet IMG-Placeholder, der zur Entwicklung benutzt werden kann.

<Preview>
  <Story name="Default MDX">
    <ImagePlaceholder
      width={text('Width', 300)}
      height={text('Height', 300)}
      colorBg={text('Color of the image (Hex)', 'f5f5f5')}
      colorText={text('Color of the dummy-Text on image (Hex)', '999')}
      imageText={text('Dummy-Text on image', 'Dummy')}
      font={select('Font-family', ['bebas', 'bebas', 'lobster', 'museo'])}
      fontSize={text('Font-size', 32)}
      alt={text('Alt-text', 'Placeholder image')}
    />
  </Story>
</Preview>

# Beispiele

<Story name="Different Sizes">
  <StyledImageStoryContainer>
    <ImagePlaceholder
      width="200"
      height="200"
      imageText="200 x 200"
      imageText="200 x 200"
      fontSize="16"
    />
    <ImagePlaceholder
      width="150"
      height="150"
      imageText="150 x 150"
      fontSize="16"
    />
    <ImagePlaceholder
      width="100"
      height="100"
      imageText="100 x 100"
      fontSize="16"
    />
    <ImagePlaceholder
      width="80"
      height="80"
      imageText="80 x 80"
      fontSize="16"
    />
    <ImagePlaceholder
      width="60"
      height="60"
      imageText="60 x 60"
      fontSize="16"
    />
  </StyledImageStoryContainer>
</Story>

# Tool - FakeImageGenerator

https://fakeimg.pl/  
https://github.com/Rydgel/Fake-images-please

<Props of={ImagePlaceholder} />

export const StyledImageStoryContainer = styled.div`
  img {
    margin: 0 15px 15px 0;
  }
`;
